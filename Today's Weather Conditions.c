#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	double today_high, today_low, today_condition, average_temp;
	
	int i; for (i = 0; i<"%d":i++)

	printf("Enter Today's High: %lf");
	scanf("%lf", &today_high);
	printf("Enter Today's Low: %lf");
	scanf("%lf", &today_low);
	printf("Enter Today's Conditions: %lf");
	scanf("%lf", today_condition)

	average_temp = today_high * today_low

	return 0;
}
